# Solution
- Sort array
- If we fix an index i, we can run the two pointers algo to get the 3 numbers
- We want to skip duplicates when we move the i pointer
- We also want to skip duplicates if we find the three numbers, as we increment the l pointer

# Time Complexity
- `O(n^2)`